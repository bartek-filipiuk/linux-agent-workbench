Continue the research task in /workspace: read README.md, data/README.md, data/progress.json and data/businesses.json first. Four categories are done (16 records). Remaining: plumber, florist, 4 businesses each, Bristol or nearby towns, found with DuckDuckGo HTML search (https://html.duckduckgo.com/html/?q=plumber+Bristol etc.). Reading only: never log in, never fill or submit forms. Progress rule: a run can pause at its configured limits, so write each business record to data/businesses.json IMMEDIATELY after checking it (append with a heredoc-driven node one-liner or by rewriting the file; keep valid JSON) and note the business in data/progress.json; visit at most 2 pages per business (contact page and price list) and never more than 6 navigations before writing results. A business whose site cannot be assessed after 2 pages gets a record with "unknown" values and a note; move on. When all 24 records exist: write build.mjs (Node, no dependencies) that validates data/businesses.json (24 records, required fields, yes/no/unknown values) and renders index.html: a summary with counts per category (booking, price list, phone, hours out of 4), a matrix table of all businesses with links to websites and sources, and a methodology note with the date; plain HTML, inline CSS, black on white with the accent #f26b1d, no JavaScript. Run node build.mjs, verify with node -e that index.html has at least 24 rows with href links and that the JSON parses, commit, and end your reply with a 5-line summary and the words PROJECT DONE. If the run ends before that, make sure progress.json says exactly where to continue.
